import 'package:flutter/material.dart';

const rescheduledTaskKey = 'com.example.eon_plus_test';
const sharedPrefEndDateKey = 'endDate';
const sharedPrefEndlessKey = 'endless';
const notificationIcon = '@drawable/flutter_logo';

const snackBarDuration = Duration(seconds: 2);

 TextStyle textStyle = const TextStyle(
    fontSize: 22,
    fontWeight: FontWeight.bold,
    color: Colors.black54
 );
